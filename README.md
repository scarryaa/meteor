# meteor

A modern editor.

## Build Instructions

1. Run this command to generate the tree-sitter bindings:

```bash
flutter pub run ffigen --config ffigen.yaml
```

2. Run the app
```bash
flutter run
```
